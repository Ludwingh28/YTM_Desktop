// Intentionally minimal: the app doesn't need to expose any Node/Electron
// APIs to the music.youtube.com page. Ad-hiding logic is injected separately
// from main.js once the page has loaded (see injectAdSkipping).
